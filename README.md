# multi-tenant-task-manager
A full-stack multi-tenant task management application where multiple organizations can manage tasks securely. The system implements JWT-based authentication, role-based access control (Admin/Member), and strict tenant isolation using organization IDs. Features include task CRUD operations, audit logging, and containerized deployment using Docker.
